function NotFound() {
  return (
    <div style={{ display: 'flex', justifyContent: 'center', alignContent: 'space-around' }}>
    <h1><span className="fw-bold">
      La página que buscas no existe
    </span>
    </h1>
    </div>
  )
}

export default NotFound
